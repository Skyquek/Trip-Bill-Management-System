name: authentication_repository
description: Dart package which manages the authentication domain.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  authentication_api:
    path: ../authentication_api

  django_backend_api:
    path: ../django_backend_api